/**
 * Problems whose main idea is an algorithm (sorting, searching, sliding
 * window, ...). Each class is registered in
 * {@link com.example.onlinejava.problem.ProblemRegistry}.
 */
package com.example.onlinejava.problem.algorithms;
