/**
 * Application users: the {@code app_user} entity, its upsert-on-login
 * service, and the {@link org.springframework.security.oauth2.core.user.OAuth2User}
 * principal that carries the database id through the security context.
 */
package com.example.onlinejava.user;
